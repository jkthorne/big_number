module BigNumber
  struct BigInt
    # TODO: Phase 2 — Group C (Basic Arithmetic)
    # +, -, *, neg, abs, addmul, submul
    # TODO: Phase 2 — Group D (Division)
    # cdiv, fdiv, tdiv (3 rounding modes), mod, divexact, divisible?, congruent?
  end
end
